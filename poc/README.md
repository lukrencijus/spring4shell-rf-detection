Exploit's proof of concept [Spring4Shell](https://github.com/reznok/Spring4Shell-POC/tree/master) made by reznok.

- Quick execution guide:
  - python3 exploit.py --url "url of the springboot server"
  - In the scope of this repository the url is "http://<base\>:<port\>/save" where base is either local host or VM's IP and port is either 8080 or port mapped port.